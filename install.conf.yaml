- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vimrc:
        path: vimrc
        force: true
    ~/.zshrc:
        path: zshrc
        force: true
    ~/.gitconfig:
        path: gitconfig
        force: true
    ~/.bash_profile:
        path: bash_profile
        force: true
    ~/.bashrc:
        path: bashrc
        force: true
    ~/.zsh:
        path: zsh/
        force: true
    ~/.tmux.conf:
        path: tmux.conf
        force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
